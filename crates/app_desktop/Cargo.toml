[package]
name = "app_desktop"
version.workspace = true
edition.workspace = true

[lints]
workspace = true

[dependencies]
app_host = { path = "../app_host" }
graph_model = { path = "../graph_model" }
plugin_api = { path = "../plugin_api" }
state_store = { path = "../state_store" }
eframe = { version = "0.34.1", default-features = false, features = ["default_fonts", "glow", "wayland", "x11"] }
rfd = "0.17.2"

[dev-dependencies]
git_service = { path = "../git_service" }
